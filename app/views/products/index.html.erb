<%= link_to "New Product", new_product_path, class: "font-basic" if authenticated?%>

<div id="products">
    <h1 class="">Articles</h1>
    <% @products.each do |product|%>
        <div class="card mb-4">
            <%= link_to product.name, product %>
        </div>
    <% end %>
</div>
